#include <iostream>
#include <cstdlib>

int main() {
    // int input;

    // std::cout << "jdkadjajdl;ajdaksjdakjsdk:\n";

    // will execute a shell command eg,. to clear the screen we will run clear command, not recommended for real applictations.
    // system("clear");

    // std::cout << "After: \n";

    while (true) {
        std::cout << "HELLO World";
        system("clear");
    }
    return 0;
}